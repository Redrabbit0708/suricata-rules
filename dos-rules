alert udp any any -> $HOME_NET any (msg:"[DoS] UDP Large-Payload Rate"; flow:to_server,stateless; dsize:>300; threshold:type both, track by_src, count 60, seconds 10; sid:9001003; rev:1;)**

alert icmp any any -> $HOME_NET any (msg:"[DoS] ICMP Echo Rate"; itype:8; threshold:type both, track by_src, count 40, seconds 10; sid:9001002; rev:1;)**

alert http any any -> $HOME_NET any (msg:"POTENTIAL DoS - High HTTP GET Rate"; flow:to_server,established; http_method; content:"GET"; threshold:type both, track by_src, count 40, seconds 10; sid:9000003; rev:2;)**

alert tcp any any -> $HOME_NET any (msg:"[DoS] TCP SYN Rate"; flow:stateless; flags:S; threshold:type both, track by_src, count 60, seconds 10; sid:9001001; rev:1;)**
